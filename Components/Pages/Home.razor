@page "/"

<PageTitle>Inicio - FacturaPop</PageTitle>

<div class="hero-container d-flex align-items-center justify-content-center text-center px-4" style="min-height: 85vh;">
    <div style="max-width: 900px;">

        <h1 class="display-3 fw-bold mb-3 text-dark animate-fade-up">
            Bienvenido a <br />
            <span style="color: var(--pop-primary);">FacturaPop</span>
        </h1>

        <p class="lead text-muted mb-5 animate-fade-up delay-1">
            Tu sistema inteligente para gestionar facturas de manera rápida y sencilla.
        </p>

        <div class="d-flex justify-content-center gap-3 animate-fade-up delay-2 mb-5">
            <a href="/facturador" class="btn btn-primary btn-lg px-5 py-3 shadow-sm rounded-pill">
                <i class="bi bi-plus-lg me-2"></i> Crear Nueva Factura
            </a>
        </div>

        <div class="row g-4 justify-content-center animate-fade-up delay-3">
            <div class="col-md-4">
                <a href="/facturas" class="text-decoration-none">
                    <div class="card p-4 h-100 border-0 shadow-sm hover-card text-center">
                        <div class="fs-1 text-primary mb-3"><i class="bi bi-clock-history"></i></div>
                        <h5 class="fw-bold text-dark">Historial</h5>
                        <p class="text-muted small mb-0">Ver facturas guardadas.</p>
                    </div>
                </a>
            </div>

            <div class="col-md-4">
                <a href="/reportes" class="text-decoration-none">
                    <div class="card p-4 h-100 border-0 shadow-sm hover-card text-center">
                        <div class="fs-1 text-secondary mb-3"><i class="bi bi-bar-chart-fill"></i></div>
                        <h5 class="fw-bold text-dark">Reportes</h5>
                        <p class="text-muted small mb-0">Consultar ingresos anuales.</p>
                    </div>
                </a>
            </div>

            <div class="col-md-4">
                <a href="/dashboard" class="text-decoration-none">
                    <div class="card p-4 h-100 border-0 shadow-sm hover-card text-center">
                        <div class="fs-1 text-info mb-3" style="color: var(--pop-dark) !important;"><i class="bi bi-speedometer2"></i></div>
                        <h5 class="fw-bold text-dark">Dashboard</h5>
                        <p class="text-muted small mb-0">Estadísticas generales.</p>
                    </div>
                </a>
            </div>
        </div>
    </div>
</div>

<style>
    .hero-container {
        background: radial-gradient(circle at 50% 50%, #f7f7ff 0%, #ffffff 100%);
    }

    .hover-card {
        transition: transform 0.3s ease, box-shadow 0.3s ease;
        background-color: white;
    }

        .hover-card:hover {
            transform: translateY(-5px);
            box-shadow: 0 10px 20px rgba(144, 103, 198, 0.1) !important;
        }

    /* Animaciones */
    .animate-fade-up {
        animation: fadeUp 0.8s ease-out forwards;
        opacity: 0;
        transform: translateY(20px);
    }

    .delay-1 {
        animation-delay: 0.1s;
    }

    .delay-2 {
        animation-delay: 0.2s;
    }

    .delay-3 {
        animation-delay: 0.3s;
    }

    @@keyframes fadeUp {
        to {
            opacity: 1;
            transform: translateY(0);
        }
    }
</style>